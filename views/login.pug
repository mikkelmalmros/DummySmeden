html
    head
        title Login til Dummysmeden
    body
        h1 Login
        form(action="/login" method="post")
            div.divUsername
                Label(for="username") Username
                input(type="text" id="usernameInput" name="username" placeholder="Username" autofocus required)
            div.divPassowrd
                Label(for="password") Password
                input(type="password" id="passwordInput" name="password" placeholder="Password" minlength="8" required)
            button(type="submit") Login